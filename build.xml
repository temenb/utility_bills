<project name="rent_calculator" default="build">

    <import file="${basedir}/build/scripts/main.xml" />

    <target name="build"
            depends="cache-clear, phpmd"
            description="Runs overall build process"/>

</project>
